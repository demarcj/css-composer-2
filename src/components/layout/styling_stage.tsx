import styles from "src/styles/styling_stage.module.css"

export const StylingStage: React.FC = () => {
  return (
    <section className={styles.styling_stage}>
      <div className={styles.header}>Styling</div>
    </section>
  )
}